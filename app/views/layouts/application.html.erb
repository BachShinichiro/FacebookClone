<!DOCTYPE html>
<html>
  <head>
    <title>FootManga</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
    <header>
    <% if logged_in? %>
      <li><%= link_to 'トップページ', root_path %></li>
      <li><%= link_to "Profile", user_path(current_user.id) %></li>
      <li><%= link_to "Logout", session_path(current_user.id), method: :delete %></li>
      <li><p class="login"><%= @current_user.name %>さん（<%= @current_user.email %>）は、現在ログイン中です<p></li>
    <% else %>
      <li><%= link_to 'トップページ', root_path %></li>
      <li><%= link_to "ログインして、画像やコメントを投稿しよう", new_user_path %></li>
      <li><%= link_to '[一覧へ]', feeds_path %></li>

      <li><p class="login">現在ログインしていません</p></li>
      <li><%= link_to "Sign up", new_user_path %></li>
      <li><%= link_to "Login", new_session_path %></li>
    <% end %>
    </header>
    <contents>
    <%= yield %>
  </contents>
    <footer>
      <p>運営会社アラサー株式会社</p>
      <p>© 2020 FootManga</p>
    </footer>
  </body>
</html>
